name: Test Python (deprecated)

on:
  workflow_dispatch:

jobs:
  test-python:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated. Use validate-all.yml for Python validation and tests."
